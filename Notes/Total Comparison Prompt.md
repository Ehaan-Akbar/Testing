You are an expert full stack engineer. Build a complete fully functional music player web application with both frontend and backend fully wired together. The app must work correctly on the first run without requiring additional fixes. Do not leave any placeholders TODOs or incomplete logic everything must already be implemented. Core Requirements. Frontend. Use React preferred or a simple alternative if it reduces complexity. Build a clean minimal UI with Play Pause button Next Previous track controls Seek bar scrubbable and updates in real time Volume control Track title and duration display Playlist view clickable to change songs. Ensure state management is fully functional and synced with backend data no fake or mock only logic. Backend. Use Nodejs with Express. Implement API endpoints to Fetch playlist array of songs with metadata Stream audio files. Serve audio files from a local directory. Properly support HTTP range requests so seeking works correctly. Integration Requirements. Frontend must fetch playlist from backend API on load. Audio playback must use backend streaming endpoints no hardcoded file paths. All controls play pause seek next previous volume must work correctly and reliably. Ensure CORS routing and ports are configured so the app runs without errors. Data and Files. Include a small working playlist 3 to 5 songs. Provide actual usable local audio files or clearly structured placeholder files that will work immediately when run. Each track must include title artist duration and file reference. Run Instructions. Provide clear steps to run the app Install dependencies Start backend server Start frontend app. The app must run locally with no extra setup or manual fixes. Code Quality. Keep everything simple readable and minimal. Avoid unnecessary dependencies or complexity. Handle basic edge cases empty playlist missing file failed playback. Output Requirements. Provide full project folder structure. Include all necessary source code files. Ensure all imports and paths are correct. Do not omit any required code. The final result must run successfully on the first attempt.