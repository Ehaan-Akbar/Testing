{
  "name": "local-music-player",
  "version": "1.0.0",
  "private": true,
  "description": "A complete React and Express local music player with HTTP range streaming.",
  "scripts": {
    "install:all": "npm install",
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "start": "concurrently \"npm run start --workspace backend\" \"npm run dev --workspace frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
